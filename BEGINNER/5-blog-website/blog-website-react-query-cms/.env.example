# Đổi tên file này thành .env.local rồi sửa giá trị bên dưới nếu muốn dùng Sanity CMS
# Nếu không set, app sẽ tự fallback sang public/posts.json

VITE_SANITY_PROJECT_ID=your_project_id
VITE_SANITY_DATASET=production
VITE_SANITY_API_VERSION=2025-01-01
